apiVersion: v1
kind: Namespace
metadata:
  name: toggle-master
---
# --- CONFIGMAP (Endereços e Configurações Não-Sensíveis) ---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: toggle-master
data:
  # Hosts RDS e Redis (Gerados dinamicamente via IaC AWS)
  auth-db-host: "rds-db-auth-service.czwzzmbvmsao.us-east-1.rds.amazonaws.com"
  flag-db-host: "rds-db-flag-service.czwzzmbvmsao.us-east-1.rds.amazonaws.com"
  targeting-db-host: "rds-db-targeting-service.czwzzmbvmsao.us-east-1.rds.amazonaws.com"

  # --- CACHE ---
  REDIS_ADDR: "elastic-cache-evaluation-service.heoiyh.0001.use1.cache.amazonaws.com"
  
  # Nomes dos Databases
  auth-db-name: "auth_db"
  flag-db-name: "flag_db"
  targeting-db-name: "targeting_db"

  # Configurações AWS Gerais
  sqs-url: "https://sqs.us-east-1.amazonaws.com/797561896734/analytics-service-sqs"
  dynamo-table: "ToggleMasterAnalytics"
  aws-region: "us-east-1"
  
  # Portas Padrão Internas
  db-port: "5432"
  db-user: "postgres"
  redis-port: "6379"

  # OpenTelemetry Collector Endpoints (namespace monitoring)
  otel-collector-grpc: "otel-collector-opentelemetry-collector.monitoring.svc.cluster.local:4317"
  otel-collector-http: "http://otel-collector-opentelemetry-collector.monitoring.svc.cluster.local:4318"
